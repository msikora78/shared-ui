define([], function() {
    'use strict';

    var data = {
        legend: 'Overview',
        description: 'Four states for input fields: default, clicked (active), error, and disabled.'
    };

    return data;
});