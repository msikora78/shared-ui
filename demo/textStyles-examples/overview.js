define(['jquery'], function($) {

    var data = {
        legend: 'Overview',
        description: 'This sampling of text styles represents a safe standard for \
            tm360 apps. However variations will arise on a design-by-design basis. \
            But colors, link styles, and text shadows will all follow this standard.'
    };

    return data;
});