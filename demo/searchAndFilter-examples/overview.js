define(['jquery'], function($) {

    var data = {
        legend: 'Overview',
        description: 'Search and Filter field that contains a hint while the value \
            is empty or an “X” icon to clear the user-entered value.'
    };

    return data;
});